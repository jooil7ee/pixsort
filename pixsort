#!/usr/bin/env bash
# pixsort 실행 래퍼 스크립트

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if ! command -v uv &>/dev/null; then
  echo "오류: uv 가 설치되어 있지 않습니다. https://docs.astral.sh/uv/" >&2
  exit 1
fi

cd "${SCRIPT_DIR}"
exec uv run python main.py "$@"
